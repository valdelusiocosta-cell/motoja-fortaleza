services:
  - type: web
    name: motoja-fortaleza
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: MOTOJA_DB
        value: /tmp/motoja.sqlite3
